import React from "react";
import styles from "./SectionLine.css";

class SectionLine extends React.Component {
    render() {
        return (
            <hr className={styles.sectionLine} />
        )
    }
}

export default SectionLine;